{
  "name": "team-pulse-board",
  "version": "0.2.0",
  "description": "A Confluence macro that finds Jira tickets related to the page you're on by extracting distinctive words from the page title and running the JQL search for you — grouped by status, with editable match words.",
  "main": "src/index.js",
  "scripts": {
    "deploy": "forge deploy",
    "lint": "eslint --ext .js,.jsx src"
  },
  "dependencies": {
    "@forge/api": "^8.0.4",
    "@forge/bridge": "^6.3.0",
    "@forge/react": "^12.1.2",
    "@forge/resolver": "^2.0.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
