{
  "name": "sprint-ready-agent",
  "version": "0.1.0",
  "description": "Sprint Ready Agent — a Rovo agent that prepares a Jira issue for sprint planning: inspects context, proposes field values with reasoning, and applies only what the user confirms.",
  "main": "src/index.js",
  "scripts": {
    "forge": "forge",
    "deploy": "forge deploy",
    "upgrade": "forge install --upgrade",
    "lint": "eslint --ext .js src"
  },
  "dependencies": {
    "@forge/api": "^8.0.4",
    "@forge/resolver": "^2.0.0"
  },
  "devDependencies": {
    "@forge/cli": "^11.0.0",
    "eslint": "^8.57.0"
  }
}